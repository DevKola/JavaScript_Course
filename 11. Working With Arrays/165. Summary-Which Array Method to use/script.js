"use strict";

// Ask the question, What do I want to from this method

/*
  // to mutate the original array:
    // Add to original Array:
      .push() -> end
      .unshift -> start

    // Remove from the original Array:
      .pop() -> end
      .shift() -> start
      .splice() -> any

    // Others: 
      .reverse() -> reverse an original array
      .sort() -> sort the original array -> Ascending/Descending
      .fill() -> fill an array

  // A new array:
    // Computed from original Array:
      .map() -> loop and gives a new array based on callback function

    // Filtered using condition:
      .filter() -> based on condition -> gives new array -> if/else

    // Portion of original:
      .slice() -> renoves and returna new array -> from one position to another

    // Adding original to another array:
      .concat() -> adding two arrays together

    // Flatening the original array:
      .flat() -> flatens an array -> nested array
      .flatMap() -> combination of flat and map -> loops and flat


  // An array index:
    // Based on value: 
      .indexOf() -> if the value exists

    // Based on test condition:
      .findIndex() -> based on a condition -> in callback function


  // An array element
    // Based on a test condition:
      .find() -> if we need the array el itself


  // Know if an array includes:
    // Based on values:
      .includes() -> if the value exists
    
    // Based on a test condition:
      .some() -> at least one condition should be true -> returns boolean
      .every() -> all consitions must be true -> returns boolean

  // A new string:
    // Based on a string:
      // .join() -> 


  // Transform to a value
    // Based on accumulator:
      .reduce() -> Boils down an array to single value of any type: number, boolean, string, or even a new array or object

  // To just loop array
    // Based on a callback:
      .forEach() -> Does not create a new array, just loops over it
*/ 